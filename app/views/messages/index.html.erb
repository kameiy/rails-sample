<% provide(:title, 'Messages') %>
<h1>Messages</h1>

<ul class="user">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, message_path(user.id) %>
    </li>
  <% end %>
</ul>